"""
Module des routeurs (Endpoints) de l'API.

Ce paquet contient tous les routeurs FastAPI de l'application, organisés par 
domaine fonctionnel (authentification, profil, exercices, simulation, etc.).
Chaque module expose une instance de `APIRouter` qui est ensuite incluse
dans l'application principale.
"""
